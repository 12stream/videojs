<template>
    <div class="video">
        <div class="return-title">
            <span>中国网正在上演</span>
        </div>
        <div class="videotab">
            <ul>
                <li  v-for="(item,i) in tabContents" :key="i" :class="currentTabsIndex == i ? 'tabs-item active' : 'tabs-item'" @click="selectTabs(i)">
                    {{item}}
                </li>
            </ul>
        </div>
        <All v-if="currentTabsIndex == 0"></All>
    </div>
</template>
<script>
import All from './components/All'
export default {
    data() {
        return {
            tabContents:['全部', '戏曲', '戏剧', '儿童剧', '音乐现场', '旅游演出', '一起看'],
            currentTabsIndex:0
        }
    },
    components:{All},
    methods:{
        selectTabs(i) {
            this.currentTabsIndex = i;
        }
    }
}
</script>
<style lang="scss">
.video {
    padding-bottom: 5rem;
    .return-title {
        display: flex;
        justify-content: center;
        height: 44px;
        background: #EDEEF1;
        span {
            font-size: 17px;
            color: #111;
            line-height: 44px;
        }
    }
    .videotab {
        min-width: 100%;
        height: 5rem;
        background: #fff;
        ul {
            width: 100%;
            height: 5rem;
            white-space: nowrap;
            overflow: scroll;
            display: flex;
            align-items: center;
            li {
                font-size: 1.3rem;
                padding: 0 2rem;
                color:#4A4A4A;
                display: inline !important;
                &.active {
                    font-size: 2.1rem; 
                    color: #FF6633;
                }
            }
        }
        ul::-webkit-scrollbar{
            display: none;
        }
    }
}
</style>

